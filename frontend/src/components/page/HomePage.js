import React, { useEffect, useState } from 'react';
import api from '../../auth/server-api';

const HomePage = () => {

  const[greeting, setGreeting] = useState('');

  return (
    <div className="home_container">
    </div>
  );
}

export default HomePage;